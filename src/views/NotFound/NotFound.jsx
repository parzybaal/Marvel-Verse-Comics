import "./NotFound.scss"

const NotFound = () => {
    return (
        <section>
            <h1>Error 404, Not Found</h1>
            <div>Lo siento, no se encontraron cómics.</div>
        </section>

    );
};

export default NotFound;